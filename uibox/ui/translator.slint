export struct Translation {
    cancel: string,
    ok: string,

    setting: string,
    help: string,
    about: string,

    setting-dialog-ui: string,
    setting-dialog-ui-font-size: string,
    setting-dialog-ui-font-family: string,
    setting-dialog-ui-language: string,

    no-data: string,
    search-placeholder: string,

    system-default-font: string,

    red: string,
    green: string,
    blue: string,
    alpha: string,
    hex: string,

    tip-copy: string,
    tip-colorpalette: string,
    tip-colormaker: string,
    tip-colorpicker: string,
    tip-systemfont: string,
    tip-setting: string,
    tip-help: string,
    tip-about: string,
    tip-pick-color: string,
    tip-zoom-in: string,
    tip-zoom-out: string,
    tip-zoom-recover: string,
    tip-remove-image: string,
    tip-load-img: string,
}

export global Translator {
    public pure function translation(is-cn: bool) -> Translation {
        return {
            cancel: is-cn ? "取消" : "Cancel",
            ok: is-cn ? "确定" : "Confirm",

            setting: is-cn ? "设  置" : "Setting",
            help: is-cn ? "帮  助" : "Help",
            about: is-cn ? "关  于" : "About",

            setting-dialog-ui: is-cn ? "界面" : "UI",
            setting-dialog-ui-font-size: is-cn ? "字体大小:" : "Font Size:",
            setting-dialog-ui-font-family: is-cn ? "字体样式:" : "Font Family:",
            setting-dialog-ui-language: is-cn ? "选择语言:" : "Language:",

            no-data: is-cn ? "没有数据" : "No Data",
            search-placeholder: is-cn ? "请输入关键字" : "Input Keywords",

            system-default-font: is-cn ? "系统默认字体" : "System Default Font",

            red: is-cn ? "红通道:" : "Red:",
            green: is-cn ? "绿通道:" : "Green:",
            blue: is-cn ? "蓝通道:" : "Blue:",
            alpha: is-cn ? "透明度:" : "Alpha:",
            hex: is-cn ? "颜色值:" : "Color:",

            tip-copy: is-cn ? "复制" : "Copy",
            tip-colorpalette: is-cn ? "颜色板" : "Color Palette",
            tip-colormaker: is-cn ? "调色板" : "Color Maker",
            tip-colorpicker: is-cn ? "取色器" : "Color Picker",
            tip-systemfont: is-cn ? "系统字体" : "System Font",
            tip-setting: is-cn ? "设置" : "Setting",
            tip-help: is-cn ? "帮助" : "Help",
            tip-about: is-cn ? "关于" : "About",
            tip-pick-color: is-cn ? "取色" : "Pick Color",
            tip-zoom-in: is-cn ? "缩小" : "Zoom In",
            tip-zoom-out: is-cn ? "放大" : "Zoom Out",
            tip-zoom-recover: is-cn ? "恢复大小" : "Reset Size",
            tip-remove-image: is-cn ? "移除图片": "Romove Image",
            tip-load-img: is-cn ? "点击加载图片" : "Click to Load Image",
        };
    }
}
