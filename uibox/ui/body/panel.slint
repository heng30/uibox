import { Theme } from "../theme.slint";
import { Store, SelectPanel } from "../store.slint";
import { ColorPalette } from "./colorpalette.slint";
import { ColorPicker } from "./colorpicker.slint";
import { ColorMaker } from "./colormaker.slint";
import { SystemFont } from "./systemfont/panel.slint";

export component Body inherits VerticalLayout {
    // in-out property<SelectPanel> selected-panel: SelectPanel.colorpalette;
    in-out property<SelectPanel> selected-panel: SelectPanel.colormaker;
    ColorPalette {
        visible: selected-panel == SelectPanel.colorpalette;
        height: self.visible ? Theme.default-height : 0px;
    }

    ColorMaker {
        visible: selected-panel == SelectPanel.colormaker;
        height: self.visible ? Theme.default-height : 0px;
    }

    ColorPicker {
        visible: selected-panel == SelectPanel.colorpicker;
        height: self.visible ? Theme.default-height : 0px;
    }

    SystemFont {
        visible: selected-panel == SelectPanel.systemfont;
        height: self.visible ? Theme.default-height : 0px;
    }
}
